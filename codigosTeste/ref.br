programa Referencia;

var a -> real;

funcao TesteRef(ref args -> real;) -> inteiro;
var y -> real;
comeca
    y = args * 7;
    args = y;
fim;

comeca
    a = 5;
    TesteRef(a);
    escreval(a);
fim.
